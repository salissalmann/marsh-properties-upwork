import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Marsh Properties",
  description: "Marsh Properties is an emerging force in the Real Estate sector in Pakistan. As authorized Sales partners for numerous prestigious residential and commercial projects across the country, we facilitate transactions totaling over 20 billion annually.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
